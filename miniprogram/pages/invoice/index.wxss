.type-row { display: flex; gap: 16rpx; }
.type-chip {
  padding: 12rpx 32rpx;
  background: var(--gray-1);
  border-radius: 32rpx;
  font-size: 26rpx;
}
.type-chip.active {
  background: var(--primary-color);
  color: #fff;
}
